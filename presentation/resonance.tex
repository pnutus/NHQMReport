\documentclass[tikz=true]{standalone}
\usepackage{../main/NHQM}

\begin{document}

   \pgfplotsset{
     width = 14cm,
     height = 8cm,
     axis lines = middle,
     xmin = -0.01, xmax = 0.5,
     enlargelimits,
     ylabel=Im $k/\b{\si{fm^{-1}}}$,
	 xlabel=Re $k/\b{\si{fm^{-1}}}$,
     every axis y label/.style={
     at = {(current axis.above origin)},
     anchor = north west,
     },
     yticklabel style={/pgf/number format/fixed,
     /pgf/number format/precision=3},
     every axis x label/.style={
     at = {(current axis.right of origin)},
     anchor = north,
     },
     every x tick label/.append style = {anchor = south, yshift = 3pt},
	 xtick={0.15,0.25,0.35,0.45},
     ymax = 0.04,
     ytick = {-0.2,-0.1},
   }

   \begin{tikzpicture}
     \begin{axis}[
        legend pos=south east,
        every axis legend/.append style={nodes={right}},
	   ]

  	   \addplot[color = red, only marks,ultra thick,mark=diamond*] 
         table {../figures/poles(contour)/res.data};
  	   \addlegendentry{Resonans}
       \addplot[color=darkgray, only marks, thick] 
         table [x index =2, y index =3] {../figures/poles(contour)/triangle.data};
       \addlegendentry{Spridda tillstånd}
       \addplot+[color=gray, no marks, ultra thick] 
         table [x index =0, y index =1] {../figures/poles(contour)/triangle.data};
       \addlegendentry{Kontur}
	   \addplot[color = white, only marks,ultra thick,] table {../figures/poles(contour)/res.data};
  	   \addplot[color = red, only marks,ultra thick,mark=diamond*] table {../figures/poles(contour)/res.data};

     \end{axis}
   \end{tikzpicture}


\end{document}