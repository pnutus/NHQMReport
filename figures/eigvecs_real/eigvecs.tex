\documentclass[tikz=true]{standalone}
\usepackage{../../main/NHQM}

\begin{document}

	    \pgfplotstableread{eigvecs_real.data}\wfs
  \pgfplotsset{
    width = \textwidth, height = 7cm,
 xlabel = $k/\b{\si{fm^{-1}}}$, ylabel = $k^2 \absq{\Phi(k)}$,
    axis x line = bottom,
    xmin = 0,
    xmax=3.075,
    ymax=1.05,
    axis y line = left,
    no markers,
    %ytick = \empty,
	xtick = {0,0.5,...,3},
    legend style={at={(0.7,0.3)}, anchor=north},,
  }

    \begin{tikzpicture}
      \begin{axis}
		  
  		 \addplot[color=red, ultra thick] table [x index= 0, y index = 9] {\wfs};
  		 \addlegendentry{Unique behavior}

        \foreach \yindex in {1,...,60}
        {   
        \addplot[color=gray] table [x index= 0, y index = \yindex] {\wfs};
        }
		\addlegendentry{Regular behavior}
		
		 \addplot[color=red, ultra thick] table [x index= 0, y index = 9] {\wfs};


      \end{axis}
    \end{tikzpicture}

\end{document}
