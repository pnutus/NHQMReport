\documentclass[tikz=true]{standalone}
\usepackage{../../main/NHQM}

\begin{document}
\tikzset{every mark/.append style={scale=0.5}}
   \pgfplotsset{
     width = 14cm,
     height = 12cm,
     axis lines = middle,
     enlargelimits,
     ylabel=Im $k/\b{\si{fm^{-1}}}$,
	 xlabel=Re $k/\b{\si{fm^{-1}}}$,
     every axis y label/.style={
     at = {(current axis.above origin)},
     anchor = north west,
     },
     yticklabel style={/pgf/number format/fixed,
     /pgf/number format/precision=3},
     every axis x label/.style={
     at = {(current axis.right of origin)},
     anchor = north,
     },
     every x tick label/.append style = {anchor = south, yshift = 3pt},
	 xtick={1,1.5,...,2.5},
	 xmax =2.6
   }
   \begin{tikzpicture}
     \begin{axis}[
       legend style={at={(0.6,0.4)}, anchor=north,legend columns=1},
	   ]
     \addlegendimage{only marks, red, mark=diamond*, mark size=6}
	   \addlegendentry{Resonance}
     \addlegendimage{only marks, darkgray, mark size=3}
     \addlegendentry{Continuum states}
       \addplot [only marks, color=darkgray] table {he6_momenta192.data};
	   \addplot [only marks, color=red, mark=diamond*, mark size=6] table {res.data};


     \end{axis}
   \end{tikzpicture}

 

\end{document}